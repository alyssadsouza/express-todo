const Header = () => {
    return (
      <nav className="header">
        <h3 className="logo">expressly to-do</h3>
        <a href="/logout">Logout</a>
      </nav>
    );
}

export default Header;
